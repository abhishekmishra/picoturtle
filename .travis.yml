matrix:
  include:
    - language: node_js
      node_js:
        - "10"
      before_install:
        - cd server
      install:
        - npm install
      script:
        - npm run pkg-local

notifications:
  email:
    - abhishekmishra3@gmail.com